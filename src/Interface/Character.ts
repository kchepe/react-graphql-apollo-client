export interface ICharacters {
  name: string;
  id: string;
  image: string;
  gender: string;
  location: { name: string };
  origin: {
    name: string;
  };
  species: string;
  type: string;
  status: string;
}
